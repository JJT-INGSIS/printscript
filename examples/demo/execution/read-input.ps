let name: string = readInput("Ingresá un nombre: ");
let value: number = readInput("Ingresá un número: ");
let enabled: boolean = readInput("Ingresá true o false: ");
println("Hola " + name);
println(value + 1);
if (enabled) {
    println("Entrada booleana correcta");
}
