package parser.grammar.ast

/**
 * Tipo base de las expresiones del AST (Binary, Literal, Identifier).
 * Marker por ahora; cuando definas los nodos, cada uno implementa Expression.
 * (Idealmente Statement y Expression deberían derivar ambos de Node.)
 */
interface Expression
